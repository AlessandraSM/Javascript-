// Declare uma variável contendo uma string
const frase = "Maçã"
console.log(typeof frase)

// Declare uma variável contendo um número dentro de uma string
const string = '20'
const frase2 = "Tenho Maçãs"
const quantidade = 20
const resposta = `Tenho ${quantidade} maçãs.`
console.log(resposta)
console.log(typeof string)

// Declare uma variável com a sua idade
const idade = 35

// Declare duas variáveis, uma com seu nome
const meuNome = "Alessandra"
const saladaDeFruta = "Abacaxi"

// e outra com seu sobrenome e some as mesmas
const meuSobrenome = "Sandeski Marmiroli"
const nomeSobrenome = meuNome + ' ' + meuSobrenome
//ou assim concatenado nomeSobrenome = `${meuNome} ${meuSobrenome}`
console.log(nomeSobrenome)

// Coloque a seguinte frase em uma variável: It's time
const time = "It's time"

// Verifique o tipo da variável que contém o seu nome
const verificarTipo = typeof meuNome;
console.log(verificarTipo)
console.log(typeof meuNome)
